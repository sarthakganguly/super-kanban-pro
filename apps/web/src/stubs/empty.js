// Stub for native-only modules on web.
// These are only imported inside Platform.OS !== 'web' branches,
// so this file is never actually executed — webpack just needs to resolve it.
module.exports = {};
